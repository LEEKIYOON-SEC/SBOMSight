{
  "ecosystem": "rpm",
  "label": "RPM 계열 (RHEL · Rocky Linux · AlmaLinux · CentOS)",
  "recommended_action": "{package} 패키지를 {fixed_version} 이상으로 업데이트",
  "recommended_action_no_fix": "{package} 패키지에 대해 공개된 수정 버전이 없어 업데이트로 해소할 수 없다. 아래 완화 방안 검토가 필요하다.",

  "precheck": [
    "대상 시스템의 배포판과 버전 확인: cat /etc/os-release",
    "현재 설치 버전 확인: rpm -q {package}",
    "해당 패키지에 의존하는 서비스 목록 확인 (재기동 대상 파악)",
    "적용 대상 배포판에 맞는 수정 버전이 존재하는지 벤더 advisory에서 확인",
    "변경 전 구성 백업 또는 스냅샷 확보"
  ],

  "online": {
    "title": "인터넷 연결 환경",
    "steps": [
      "dnf check-update {package}",
      "dnf upgrade {package}",
      "필요 시 서비스 재기동: systemctl restart <service>"
    ]
  },

  "airgapped": {
    "title": "폐쇄망 환경",
    "note": "인터넷이 차단된 구간에서는 패키지를 직접 내려받을 수 없다. 인터넷 가능 구간에서 의존성까지 함께 확보해 반입한 뒤 로컬 설치한다.",
    "steps": [
      "인터넷 가능 구간에서 대상 RPM과 의존성을 함께 내려받는다 (대상과 동일한 배포판·아키텍처에서 수행):\n    dnf download --resolve --alldeps --destdir ./rpms {package}",
      "매체로 반입한다.",
      "무결성과 서명을 확인한다:\n    sha256sum -c checksums.txt\n    rpm -K ./rpms/*.rpm            # 서명 검증 (NOT OK 가 나오면 설치하지 않는다)\n    rpm -qa gpg-pubkey             # 벤더 GPG 키가 등록되어 있는지 확인",
      "대상 서버에 업로드한 뒤 설치한다.\n  ㆍ 단건이면:  dnf localinstall ./rpms/*.rpm\n  ㆍ 다건이거나 반복 배포한다면 로컬 저장소를 구성한다:\n        createrepo_c ./rpms\n        cat > /etc/yum.repos.d/local-patch.repo <<'REPO'\n        [local-patch]\n        name=Local patch repository\n        baseurl=file:///path/to/rpms\n        enabled=1\n        gpgcheck=1\n        REPO\n        dnf --disablerepo='*' --enablerepo='local-patch' upgrade {package}",
      "설치 결과를 확인하고 필요한 서비스를 재기동한다."
    ]
  },

  "verification": [
    "설치 버전 확인: rpm -q {package}   (기대값: {fixed_version} 이상)",
    "패키지 변경 이력 확인: dnf history list | head",
    "관련 서비스 정상 동작 확인: systemctl status <service>",
    "SBOM을 다시 생성해 재스캔하고 해당 CVE가 사라졌는지 확인:\n    syft <대상> -o cyclonedx-json=sbom.json\n    grype sbom:sbom.json -o json | grep {cve}"
  ],

  "mitigation_when_no_fix": [
    "해당 기능·모듈을 사용하지 않는다면 비활성화하거나 제거를 검토한다.",
    "노출 경로를 좁힌다 — 네트워크 접근 통제, 방화벽 규칙, 서비스 바인딩 주소 제한.",
    "벤더의 백포트 패치 또는 대체 패키지 제공 여부를 주기적으로 확인한다.",
    "탐지 규칙(IDS/IPS/WAF)으로 악용 시도를 관제 대상에 포함한다.",
    "수정 버전이 공개되면 즉시 적용할 수 있도록 대상 목록에 등재해 둔다."
  ]
}
