# SBOMSight 작업 규칙

이 파일은 **내가 같은 실수를 반복해서** 생겼다. 각 항목 아래에 무엇을 놓쳤는지
적어 둔다 — 이유를 지우면 규칙만 남고, 규칙만 남으면 다음에 또 건너뛴다.

---

## 시작하기 전

### 1. 고칠 파일과, 그 파일에 기대고 있는 것을 먼저 읽는다

**추측으로 인터페이스를 가정하지 않는다.** 메서드 이름·열 이름·설정 키를
"아마 이럴 것" 으로 쓰면 컴파일은 지나가고 화면에서 터진다.

- 파일 하나를 고치기 전에 그 파일을 **끝까지** 읽는다. `grep` 으로 본 몇 줄은
  읽은 것이 아니다.
- 그 파일을 부르는 쪽과, 그 파일이 부르는 쪽을 찾아 읽는다.
  `grep -rn "<이름>" src/` 로 실제 사용처를 센다.
- 설정 값을 바꾸면 그 값을 읽는 코드·스크립트·문서를 전부 찾는다.

> 드라이버를 바꾸면서 `FindingRepository` 의 "DB 마다 다르게 군다" 는 주석을
> 읽지 않았고, 설치 문서가 아직 옛 DB 를 설치하라고 말하고 있는 것도
> 나중에야 알았다.

### 2. 요구가 여럿이면 먼저 체크리스트로 적는다

작업을 시작하기 전에 **요구를 번호로 나열**하고, 끝나면 **항목마다 반영 여부를
근거(`파일:줄`)와 함께** 답한다. "전부 반영했습니다" 는 근거가 아니다.

> "전부 반영했다" 고 보고한 뒤 시안 다섯 장 중 두 장을 아예 안 만든 것이
> 드러났다. 항목별로 답했으면 보고하는 그 자리에서 내가 먼저 알았다.

---

## 고치는 동안

### 3. 계획에 없던 파일에 손이 가면, 손대기 전에 말한다

범위가 늘어나는 것 자체는 문제가 아니다. **말없이 늘어나는 것**이 문제다.
왜 그 파일까지 건드려야 하는지 한 줄로 밝히고 진행한다.

### 4. 사양이 모호할 때

- 합리적인 해석이 **하나뿐이면** 그 해석을 명시하고 **진행한다.**
- 해석이 갈리고 **되돌리기 비용이 크면** 그때만 멈추고 묻는다.
  (스키마 변경 · 데이터 이관 · 화면 구조 · 어휘)

묻기만 하고 아무것도 안 내놓는 것도, 묻지 않고 큰 것을 뒤집는 것도 둘 다 나쁘다.

---

## 끝났다고 말하기 전

### 5. 돌리고, 결과를 붙인다

**실행하지 않았으면 "완료" 라고 말하지 않는다.** 결과 숫자를 그대로 옮긴다.

```bash
./mvnw -B test                                       # 컴파일 + 시험 (H2)
DB_PORT=13306 DB_USER=… ./scripts/check-mariadb.sh   # 진짜 DB + Flyway 스키마
DB_PORT=13306 DB_USER=… ./scripts/check-migrations.sh # 마이그레이션만 (스키마 건드렸으면)
```

- `mvnw test` 가 컴파일까지 한다. 따로 타입체크 명령은 없다.
- 시험 수가 줄었으면 왜 줄었는지 밝힌다. "통과" 만 쓰지 않는다.

### 6. 그 기능을 실제로 써 본다

시험 통과는 "끝났다" 가 아니다. **띄워서 직접 해 본다.**

- 화면을 고쳤으면 그 화면을 연다.
- 로그인·세션·DB 를 건드렸으면 **로그아웃하고 다시 로그인한다.**
- 업로드를 건드렸으면 진짜 파일을 올린다.
- 검사를 건드렸으면 진짜 grype 으로 돌린다.

> 검사 진행 표시를 만들고 "완료" 라고 했는데 폴링이 없어 화면이 멈춰
> 있었다. 로그인 화면을 다시 칠하고 "완료" 라고 했는데, 로그아웃 후 재로그인이
> 글꼴 파일 다운로드로 끝나고 있었다. 둘 다 한 번만 해 봤으면 즉시 보였다.

### 7. 시안이 있으면 나란히 놓고 대조한다

승인받은 시안(`docs/preview.html`)이 있으면 **화면마다 하나씩 열어 좌우로
비교한다.** 내가 만든 것만 보면 내가 만든 것은 언제나 맞아 보인다.

- 시안에 있는데 앱에 **없는 화면**이 있는지 먼저 센다.
- 색·글꼴만 옮기고 화면을 안 만든 것은 "디자인 적용" 이 아니다.

> 시안 다섯 장 중 두 장(구역 현황·검사 진행)을 아예 만들지 않고 "디자인 적용
> 완료" 라고 보고했다. 그중 하나는 재설계를 시작한 원인이었다.

### 8. 시험이 진짜 그 길을 지나가는지 본다

`.with(user(...))` 는 인증된 주체를 곧바로 꽂는다. **로그인·튕김·돌아갈 자리
기억·로그아웃·세션은 한 번도 지나가지 않는다.**

- 인증·세션·리다이렉트를 건드렸으면 `LoginFlowTest` 처럼 **실제 필터 체인**
  으로 도는 시험을 쓴다.
- 새 시험을 쓰면 **고치기 전 코드에서 먼저 돌려 실패를 확인한다.** 실패하지
  않는 시험은 아무것도 지키지 못한다.

> 화면 시험 198개가 전부 통과한 채로 로그인이 깨져 있었다.

---

## 이 저장소에서 특히

### 9. 정적 파일을 추가하면 보안 설정을 함께 본다

`src/main/resources/static/` 아래에 무엇을 더하면 `SecurityConfig.PUBLIC` 도
함께 본다. 로그인 화면이 부르는 것이 막혀 있으면, 그 요청이 '로그인 뒤 돌아갈
자리' 로 기억되어 **로그인에 성공한 순간 그 파일이 내려받아진다.**

`PageRequestCache` 가 이제 화면이 아닌 요청은 기억하지 않지만, 그것은 두 번째
방어선이다. 공개 목록이 첫 번째다.

### 10. 스키마를 바꾸면 마이그레이션을 진짜 DB 에 태워 본다

시험은 H2 로 돌고 `ddl-auto=create-drop` 이라 **`db/migration/*.sql` 은 시험에서
한 번도 실행되지 않는다.** 빈 DB 와 **데이터가 있는 DB** 양쪽에 태운다 —
자리를 옮기는 마이그레이션은 빈 DB 에서만 통과하는 일이 흔하다.

### 11. grype 의 판정을 바꾸지 않는다

이 도구는 grype 이 낸 값을 보관·정렬·집계할 뿐이다. 심각도를 다시 나누거나
설치 버전과 수정 버전을 다시 비교하지 않는다. 없는 값은 `—` 로 둔다 — 0 이나
false 로 채우면 아무도 확인하지 않은 판정이 화면에 뜬다.

grype 이 낸 것은 **탐지**, 우리가 적는 것은 **검토 결과**다. 둘을 같은 말로
부르지 않는다.

### 12. 화면에 쓰는 말은 내가 지어내지 않는다

그 팀이 실제로 쓰는 말을 쓴다. **한 가지를 가리키는 말은 하나만** 쓴다.
줄여서 뜻이 흐려지면 줄이지 않는다.

- 새 글자를 화면에 쓰기 전에 `docs/rework-plan.md` §4.1 의 어휘표를 본다.
- **시스템이 모르는 것을 아는 척하지 않는다.** 사람이 자유롭게 적는 칸에 통제를
  뜻하는 이름을 붙이지 않는다. 시스템이 아는 값(로그인 계정·시각)은 자동으로 남긴다.

> `다시 볼 날` · `감춤` · `바로 닿음` · `뜻` · `줄` · `한 일` 을 화면에 그대로 내보냈다.
> 같은 것을 `서버`/`자산`, `컴포넌트`/`패키지`, `재검사`/`다시 검사` 로 섞어 불렀다.
> `승인한 사람` 이라는 칸을 두었는데 승인 절차는 없었다 — 아무나 아무 이름이나
> 적을 수 있는 칸이었고, 통제가 있는 것처럼 보이는 만큼 없느니만 못했다.

---

## 보고할 때

- **안 한 것을 먼저 말한다.** 물어보기 전에.
- **"확인했다" 는 무엇을 어떻게 확인했는지와 함께** 쓴다. 숫자를 그대로 옮긴다.
- 요구가 여럿이었으면 **항목마다 `파일:줄` 로** 답한다 (규칙 2).
- 하다가 발견한 남의 버그는 **고치지 말고 적어 둔다.** 한 커밋에 한 가지만
  들어가야 되돌릴 수 있다.
- 불필요한 설명을 붙이지 않는다.

---

## 자주 쓰는 것

```bash
./mvnw -B test                                # 컴파일 + 시험 (H2)
DB_PORT=13306 ./scripts/check-mariadb.sh      # 진짜 DB + Flyway 스키마
DB_PORT=13306 ./scripts/check-migrations.sh   # 마이그레이션만
./scripts/run-server.sh --check               # 준비 상태
```

| 무엇 | 어디 |
|---|---|
| **지금 하고 있는 개편** | **`docs/rework-plan.md`** |
| **승인받은 화면 시안** | **`docs/preview.html`** |
| 설치 절차 | `docs/windows-setup.md` |
| 폐쇄망 운영 | `docs/offline-operations.md` |
| 설계에서 지키는 것 | `README.md` |
