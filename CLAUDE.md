# SBOMSight 작업 규칙

이 파일은 **내가 같은 실수를 반복해서** 생겼다. 각 항목 아래에 무엇을 놓쳤는지
적어 둔다 — 이유를 지우면 규칙만 남고, 규칙만 남으면 다음에 또 건너뛴다.

---

## 1. 끝났다고 말하기 전에 그 기능을 실제로 써 본다

시험 통과는 "끝났다" 가 아니다. **띄워서 그 기능을 직접 해 본다.**

- 화면을 고쳤으면 그 화면을 연다.
- 로그인을 건드렸으면 **로그아웃하고 다시 로그인한다.**
- 업로드를 건드렸으면 진짜 파일을 올린다.
- 검사를 건드렸으면 진짜 grype 으로 돌린다.

> 검사 진행 표시를 만들고 "완료" 라고 했는데 폴링이 없어 화면이 멈춰
> 있었다. 로그인 화면을 다시 칠하고 "완료" 라고 했는데, 로그아웃 후 재로그인이
> 글꼴 파일 다운로드로 끝나고 있었다. 둘 다 한 번만 해 봤으면 즉시 보였다.

## 2. 시안이 있으면 시안과 나란히 놓고 대조한다

승인받은 시안이 있으면 **화면마다 하나씩 열어 좌우로 비교한다.** 내가 만든
것만 보면 내가 만든 것은 언제나 맞아 보인다.

- 시안에 있는데 앱에 **없는 화면**이 있는지 먼저 센다.
- 색·글꼴만 옮기고 화면을 안 만든 것은 "디자인 적용" 이 아니다.

> 시안 다섯 장 중 두 장(구역 현황·검사 진행)을 아예 만들지 않고 "디자인 적용
> 완료" 라고 보고했다. 그중 하나는 재설계를 시작한 원인이었다.

## 3. 시험이 진짜 그 길을 지나가는지 본다

`.with(user(...))` 는 인증된 주체를 곧바로 꽂는다. **로그인·튕김·돌아갈 자리
기억·로그아웃·세션은 한 번도 지나가지 않는다.**

- 인증·세션·리다이렉트를 건드렸으면 `LoginFlowTest` 처럼 **실제 필터 체인**
  으로 도는 시험을 쓴다.
- 새 시험을 쓰면 **고치기 전 코드에서 먼저 돌려 실패를 확인한다.** 실패하지
  않는 시험은 아무것도 지키지 못한다.

> 화면 시험 198개가 전부 통과한 채로 로그인이 깨져 있었다.

## 4. 정적 파일을 추가하면 보안 설정을 함께 본다

`src/main/resources/static/` 아래에 무엇을 더하면 `SecurityConfig.PUBLIC` 도
함께 본다. 로그인 화면이 부르는 것이 막혀 있으면, 그 요청이 '로그인 뒤 돌아갈
자리' 로 기억되어 **로그인에 성공한 순간 그 파일이 내려받아진다.**

`PageRequestCache` 가 이제 화면이 아닌 요청은 기억하지 않지만, 그것은 두 번째
방어선이다. 공개 목록이 첫 번째다.

## 5. 스키마를 바꾸면 마이그레이션을 진짜 DB 에 태워 본다

시험은 H2 로 돌고 `ddl-auto=create-drop` 이라 **`db/migration/*.sql` 은 시험에서
한 번도 실행되지 않는다.**

```bash
DB_PORT=13306 ./scripts/check-migrations.sh   # 빈 DB + 데이터 있는 DB
DB_PORT=13306 ./scripts/check-mariadb.sh      # 전체 시험을 MySQL + Flyway 스키마로
```

## 6. grype 의 판정을 바꾸지 않는다

이 도구는 grype 이 낸 값을 보관·정렬·집계할 뿐이다. 심각도를 다시 나누거나
설치 버전과 수정 버전을 다시 비교하지 않는다. 없는 값은 `—` 로 둔다 — 0 이나
false 로 채우면 아무도 확인하지 않은 판정이 화면에 뜬다.

## 7. 보고할 때

- 안 한 것은 안 했다고 먼저 말한다. 물어보기 전에.
- "확인했다" 는 **무엇을 어떻게** 확인했는지와 함께 쓴다.
- 불필요한 설명을 붙이지 않는다.

---

## 자주 쓰는 것

```bash
./mvnw -B test                                # H2
DB_PORT=13306 ./scripts/check-mariadb.sh      # MySQL + Flyway 스키마
DB_PORT=13306 ./scripts/check-migrations.sh   # 마이그레이션만
./scripts/run-server.sh --check               # 준비 상태
```

| 무엇 | 어디 |
|---|---|
| 설치 절차 | `docs/windows-setup.md` |
| 폐쇄망 운영 | `docs/offline-operations.md` |
| 설계에서 지키는 것 | `README.md` |
